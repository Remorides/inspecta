/// Generic enum to show data loading status
enum LoadingStatus {
  /// default value
  initial,
  /// loading data in progress
  inProgress,
  /// loading finished successfully
  done,
  /// there was a problem
  failure,
}
